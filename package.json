{
  "name": "init-intra",
  "version": "1.0.0",
  "description": "Init Intra — Sistema interno de gestão (clientes, pendências, visitas)",
  "private": true,
  "scripts": {
    "build": "node tools/build-config.js",
    "deploy": "vercel --prod",
    "dev": "vercel dev",
    "preview": "vercel",
    "test": "vitest run",
    "lint": "eslint js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "eslint": "^9.20.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "vitest": "^3.0.5"
  }
}
